/* Carga fragmentos HTML en elementos con data-include="ruta" */

// include.js deshabilitado para evitar duplicar partials (header/footer) — la inclusión la gestiona /js/script.js
document.addEventListener('DOMContentLoaded', function () {
  // no-op: script.js realizará la inserción de partials de forma controlada
});
